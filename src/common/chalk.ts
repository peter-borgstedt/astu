import chalk from 'chalk';

const red = chalk.red;
const blue = chalk.blue;
const green = chalk.green;
const yellow = chalk.yellow;
const orange = chalk.rgb(235, 220, 35);
const pink = chalk.rgb(255, 204, 255);
const cyan = chalk.cyan;
const magenta = chalk.magenta;
const white = chalk.white.italic;

export { red, blue, green, yellow, orange, pink, cyan, magenta, white };
